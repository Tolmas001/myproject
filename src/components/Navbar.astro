---
import { ShoppingBag } from 'lucide-astro';
---

<nav class="container py-6 flex items-center justify-between relative z-50">
  <!-- Logo -->
  <a href="/" class="text-2xl font-bold font-display">
    <span class="text-primary">pizza</span>shop<span class="text-primary">.</span>
  </a>

  <!-- Navigation -->
  <div class="hidden md:flex items-center gap-12 text-sm font-medium">
    <a href="#" class="text-primary relative after:content-[''] after:absolute after:-bottom-2 after:left-1/2 after:-translate-x-1/2 after:w-1 after:h-1 after:rounded-full after:bg-primary">Home</a>
    <a href="#" class="text-muted hover:text-light transition-colors">Menu</a>
    <a href="#" class="text-muted hover:text-light transition-colors">Events</a>
    <a href="#" class="text-muted hover:text-light transition-colors">About us</a>
  </div>

  <!-- Actions -->
  <div class="flex items-center gap-4">
    <a href="#" class="hidden md:inline-flex items-center justify-center px-6 py-2 rounded-full text-white text-sm font-medium bg-gradient-to-r from-[#FF914D] to-[#FF6B00] shadow-lg shadow-primary/20 hover:shadow-primary/40 transition-all hover:-translate-y-0.5">
      Log in
    </a>
    <button class="w-10 h-10 rounded-full border border-white/10 flex items-center justify-center text-white hover:bg-white/10 transition-colors bg-primary/10 border-primary/20 text-primary">
      <ShoppingBag size={18} />
    </button>
  </div>
</nav>
